apiVersion: v1
kind: Service
metadata:
  name: station-service
  namespace: emerald-charge
spec:
  selector:
    app: station-service
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP
